'use strict';

const es = require('../config/index'),
      config = require('../config/config'),
      dplaConfig = require('../config/config-dpla-feed'),
      Collections = require('../collections/service');

exports.getObjectArray = function(callback) {
	let objectArray = [];

	// Get collection pid/title array

	// Get all objects (type is object) from repo (ES query here)

	// Loop objects, create objects for objectArray

	callback(null, objectArray);
}